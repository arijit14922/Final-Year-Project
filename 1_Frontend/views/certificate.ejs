<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate</title>

    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

    <style>
        body {
            font-family: 'Georgia', serif;
            background: #f4f4f4;
            margin: 0;
            padding: 50px;
        }

        .navbar {
            display: flex;
            justify-content: flex-end;
            gap: 15px;
            margin-bottom: 30px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .navbar a {
            text-decoration: none;
            background-color: #4e67e8;
            color: white;
            padding: 10px 18px;
            border-radius: 6px;
            font-weight: bold;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .navbar a:hover {
            background-color: #008218;
        }

        .navbar button.print-btn {
            background-color: #4e67e8;
            color: white;
            font-size: 18px;
            font-weight: bold;
            padding: 10px 18px;
            border-radius: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: background-color 0.3s ease;
        }

        .navbar button.print-btn:hover {
            background-color: #008218;
        }

        .certificate-container {
            width: 900px;
            margin: auto;
            background: #fff;
            border: 12px solid #3e9a8c;
            padding: 60px;
            box-shadow: 0 0 45px rgba(0, 0, 0, 0.342);
            position: relative;
            overflow: hidden;
        }

        .certificate-border {
            border: 6px solid #3498db;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .certificate-border::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('educationlogocertificate.png');
            background-repeat: no-repeat;
            background-position: center;
            background-size: 400px auto;
            opacity: 0.1;
            z-index: 0;
        }

        .certificate-border>* {
            position: relative;
            z-index: 1;
        }

        .certificate-title {
            text-align: center;
            font-size: 40px;
            font-weight: bold;
            color: #4172a3;
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        .certificate-subtitle {
            text-align: center;
            font-size: 22px;
            color: #34495e;
            margin-bottom: 30px;
        }

        .recipient-name {
            text-align: center;
            font-size: 30px;
            font-weight: bold;
            color: #000;
            margin: 30px 0 10px;
            margin-top: 8%;
        }

        .description {
            text-align: center;
            font-size: 18px;
            margin: 10px 0 40px;
        }

        .details {
            text-align: center;
            font-size: 16px;
            margin-bottom: 40px;
            color: #555;
        }

        .certificate-id {
            text-align: right;
            font-size: 14px;
            color: #888;
            font-style: italic;
            margin-top: -30px;
            margin-bottom: 30px;
        }

        .signature-section {
            display: flex;
            justify-content: space-between;
            margin-top: 60px;
        }

        .signature {
            text-align: center;
        }

        .signature-line {
            border-top: 1px solid #000;
            width: 200px;
            margin: auto;
        }

        .signature-name {
            margin-top: 10px;
        }

        .date {
            text-align: left;
            margin-top: 50px;
            font-size: 16px;
            font-style: italic;
        }

        .badge2 {
            position: absolute;
            bottom: 5px;
            right: 40px;
            width: 140px;
            height: auto;
        }

        /* Print Styles */
        @media print {
            @page {
                size: landscape;
                margin: 10;
                margin-left: 7%;
            }

            body {
                padding: 0;
                background: none;
                width: 90%;
            }

            .navbar,
            .print-btn {
                display: none;
            }

            .certificate-container {
                width: 100%;
                padding: 20px;
                box-shadow: none;
                margin: 0;
                border: 12px solid #3e9a8c;
                page-break-before: always;
            }

            .certificate-border {
                padding: 40px;
                border: 6px solid #3498db;
                box-shadow: none;
                position: relative;
                overflow: hidden;
            }

            .certificate-border::before {
                content: "";
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-image: url('educationlogocertificate.png');
                background-repeat: no-repeat;
                background-position: center;
                background-size: 400px auto;
                opacity: 0.1;
                z-index: 0;
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            .certificate-border>* {
                position: relative;
                z-index: 1;
            }

            .certificate-title {
                font-size: 40px;
            }

            .recipient-name {
                font-size: 30px;
            }

            .description {
                font-size: 18px;
            }

            .details {
                font-size: 16px;
            }

            .date {
                font-size: 16px;
            }

            .certificate-id {
                font-size: 14px;
            }

            .badge2 {
                position: absolute;
                bottom: 5px;
                right: 40px;
                width: 140px;
                height: auto;
            }
        }
    </style>
</head>

<body>
    <!-- Styled Navbar with Profile and Print Button -->
    <div class="navbar">

        <button class="print-btn" onclick="window.print()">
            <i class="fa-solid fa-print"></i> <b>Print</b>
        </button>

        <a href="/profile"><i class="fa-solid fa-user"></i> <b>Profile</b></a>

        <a href="/logout"><i class="fa-solid fa-right-from-bracket"></i> <b>Logout</b> </a>
    </div>

    <!-- Certificate Content -->
    <div class="certificate-container">
        <div class="certificate-border">
            <!-- <div class="certificate-id">Certificate ID: CERT-2025-00123</div> -->

            <div class="certificate-id">Certificate ID: <%= certificateId %>
            </div>

            <div class="certificate-title">Certificate of Achievement</div>
            <br><br><br><br>

            <div class="certificate-subtitle">This is to proudly certify that</div>

            <!-- Recipient Name -->
            <div class="recipient-name">
                <%= user.username %>
            </div>

            <br>

            <div class="description">
                has successfully completed the course with <b>
                    <%= average %>%
                </b> from <b>EduStream</b>
            </div>

            <div class="details">
                Completion Date: <span id="completion-date"></span>
            </div>
            <br>

            <div class="date">Date Issued: <span id="issued-date"></span></div>

            <!-- Certification Badge -->
            <img src="certifiedbadge.png" alt="Certification Badge" class="badge2">
        </div>
    </div>

    <script>
        // Format today's date as "Month Day, Year"
        const today = new Date();
        const options = { year: 'numeric', month: 'long', day: 'numeric' };
        const formattedDate = today.toLocaleDateString(undefined, options);

        // Set both date fields
        document.getElementById('completion-date').textContent = formattedDate;
        document.getElementById('issued-date').textContent = formattedDate;
    </script>
</body>

</html>